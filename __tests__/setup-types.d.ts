// Registers @testing-library/jest-dom matchers (toBeInTheDocument, etc.)
// with TypeScript for the test suite. jest.setup.js performs the runtime
// registration; this file provides the compile-time types.
import '@testing-library/jest-dom';
